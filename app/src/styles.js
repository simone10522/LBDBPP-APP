import { StyleSheet } from 'react-native';

const baseStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#f1f1f0', // Your desired background color
  },
});

export default baseStyles;